<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.jm.dao.TokenDao">

    <insert id="addToken" parameterType="com.test.jm.dto.TokenDTO">
          INSERT INTO
          user_token
          (user_id, token, status, expire_time, create_time, modify_time)
          VALUES
          (#{user_id}, #{token},0 , #{expire_time}, now(), now());
    </insert>

    <select id="findTokenByUserId" parameterType="com.test.jm.dto.TokenDTO"
            resultType="com.test.jm.dto.TokenDTO">
        SELECT id, username, telno, add_date FROM user_token
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="user_id != null">
                user_id = #{user_id}
            </if>
        </trim>
        ORDER BY  modify_time DESC
        LIMIT 1;
    </select>

    <update id="updateToken" parameterType="com.test.jm.dto.TokenDTO">
        UPDATE user_token
        <set>
            <if test="status != null">
                status = #{status},
            </if>
                modify_date = now(),
        </set>
        WHERE user_id = #{user_id}
        and token = #{token};
    </update>

</mapper>